# A6 — hierarchy off: HSLM -> flat softmax (species-only).
# (C09 reference with exactly hslm changed.)
# Also the midpoint of the loss chain C09 -> A6 -> A8: A6 vs A8 is the
# single-factor CB-Focal comparison (see A8_plain_ce.yaml).
run_id: A6
name: flat_softmax
priority: core
description: 'C09 reference with one change: hierarchy off: HSLM -> flat softmax (species-only).'
model_family: dino
backbone: dinov3
context_levels: 3
attention_depth: 1
unfreeze_blocks: 0
hslm: false
loss: cbfocal
sampler: random            # matches the C09 reference (random sampler)
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
seeds:
- 0
- 1
- 2
