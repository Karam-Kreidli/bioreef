# A5 — hierarchy off: HSLM -> flat softmax (species-only).
# (C09 reference with exactly hslm changed.)
run_id: A5
name: flat_softmax
priority: core
description: 'C09 with one change: hierarchy off: HSLM -> flat softmax (species-only).'
model_family: dino
backbone: dinov3
context_levels: 3
attention_depth: 1
unfreeze_blocks: 0
hslm: false
loss: cbfocal
sampler: balanced
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
seeds:
- 0
- 1
- 2
