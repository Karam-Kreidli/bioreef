# C08 — MATANet (official repo, OzFish split). Keystone prior-work comparison.
#
# MATANet shares this work's multi-context architecture, so it is NOT built here
# and NOT a single-crop timm baseline. It is run FROM THE OFFICIAL REPO on our
# split via the bridge in matanet/ (export -> patch -> their B1/C1 -> ingest).
# Full workflow + attribution + pinned commit: matanet/README.md.
run_id: C08
name: matanet
priority: core
description: >
  MATANet run from the official repo on the OzFish deployment-grouped split.
  DINOv2-large fine-tuned, multi-context attention, 3-level hierarchy (matching
  our HSLM). Do NOT quote their FathomNet numbers — run their code on our split.

model_family: matanet
seeds: [0, 1, 2]
