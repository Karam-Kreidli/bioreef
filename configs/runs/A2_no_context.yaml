# A2 — context off: MCEAM removed (head on pooled ROI). Keeps HSLM+CB-Focal+balanced.
# (C09 reference with exactly context_levels changed.)
run_id: A2
name: no_context
priority: core
description: 'C09 with one change: context off: MCEAM removed (head on pooled ROI).
  Keeps HSLM+CB-Focal+balanced.'
model_family: dino
backbone: dinov3
context_levels: 0
attention_depth: 1
unfreeze_blocks: 0
hslm: true
loss: cbfocal
sampler: balanced
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
seeds:
- 0
- 1
- 2

# Frozen backbone + no context -> cache ROI [CLS] once, train only the head
# (minutes, not hours). Un-augmented; reused across seeds. See loop.probe_cache_eligible.
cache_features: true
