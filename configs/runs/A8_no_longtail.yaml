# A8 — all long-tail handling off: (CB-Focal+balanced) -> (CE+random).
# (C09 reference with exactly hslm, loss, sampler changed.)
run_id: A8
name: no_longtail
priority: core
description: 'C09 with one change: all long-tail handling off: (CB-Focal+balanced)
  -> (CE+random).'
model_family: dino
backbone: dinov3
context_levels: 3
attention_depth: 1
unfreeze_blocks: 0
hslm: false
loss: ce
sampler: random
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
seeds:
- 0
- 1
- 2
