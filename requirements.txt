# Core. Needs Python >=3.9 (transformers >=4.53 dropped 3.8).
torch>=2.1
torchvision>=0.16
transformers>=4.56.0,<4.57  # Lower: released DINOv3 support starts at 4.56.0
                          # (Meta's dinov3 repo); 4.53-4.55 raise "does not
                          # recognize architecture dinov3_vit". Upper: 4.57+
                          # imports torch.distributed.tensor.DTensor, which does
                          # not exist in torch 2.4 (pinned for this box's old
                          # glibc) -> ImportError. Weights are GATED: authenticate
                          # + get access approved before loading.
timm>=1.0                 # CNN/transformer baselines (C03/C05/C07)
numpy>=1.24
pandas>=2.0
opencv-python>=4.8
scikit-learn>=1.3
tqdm>=4.65
pyyaml>=6.0

# Optional (analysis / figures)
matplotlib>=3.7
seaborn>=0.12

# Tests
pytest>=7.4
