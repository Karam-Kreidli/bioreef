run_id: C04
name: efficientnetv2_s
priority: optional
description: 'Efficiency-oriented CNN baseline: ImageNet-pretrained EfficientNetV2-S
  fine-tuned end-to-end on the ROI crop. Flat CE, random sampler. Breadth for the
  fine-tuned tier alongside C03 (ResNet-50) and C05 (ConvNeXt-T).

  '
model_family: timm
timm_name: tf_efficientnetv2_s
pretrained: true
hslm: false
loss: ce
sampler: random
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
cache_features: false
augment: true
seeds:
- 0
- 1
- 2
