run_id: C03
name: resnet50
priority: core
description: 'Classic CNN baseline: ImageNet-pretrained ResNet-50 fine-tuned end-to-end
  on the ROI crop (same crop the DINO family sees). Flat CE, random sampler.

  '
model_family: timm
timm_name: resnet50
pretrained: true
hslm: false
loss: ce
sampler: random
family_weight: 3.0
genus_weight: 2.0
species_weight: 1.0
epochs: 60
warmup_epochs: 3
batch_size: 32
lr: 0.0001
cache_features: false
augment: true
seeds:
- 0
- 1
- 2
